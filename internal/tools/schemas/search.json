{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "search",
  "type": "object",
  "additionalProperties": false,
  "required": ["pattern"],
  "properties": {
    "pattern": {"type": "string", "description": "RE2 regular expression matched against each line."},
    "path": {"type": "string", "description": "File or directory to search, relative to the workspace root (default \".\")."},
    "max_results": {"type": "integer", "minimum": 1, "maximum": 1000, "description": "Stop after this many matches (default 100)."}
  }
}
