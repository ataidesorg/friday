{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "run_command",
  "type": "object",
  "additionalProperties": false,
  "required": ["argv"],
  "properties": {
    "argv": {"type": "array", "minItems": 1, "items": {"type": "string"}, "description": "Program and arguments; no shell, no globbing. Must match an allowed command prefix."},
    "dir": {"type": "string", "description": "Working directory relative to the workspace root (default \".\")."},
    "timeout_secs": {"type": "integer", "minimum": 1, "maximum": 600, "description": "Kill the command after this many seconds (default: sandbox wall clock)."}
  }
}
